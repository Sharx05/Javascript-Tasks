// Question 3: Create a function that accepts an array of numbers and prints each number.

function print(numbers) {
  for(var i=0;i<numbers.length;i++)
    console.log(numbers[i]);
}
print([1, 2, 3, 4, 5]); 
